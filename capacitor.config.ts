import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'minigolf-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
